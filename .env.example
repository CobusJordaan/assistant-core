# Ollama connection
OLLAMA_URL=http://localhost:11434
DEFAULT_MODEL=llama3.2

# Billing Assistant API (read-only)
BILLING_ASSISTANT_BASE_URL=https://your-billing-host.com/billing
BILLING_ASSISTANT_API_TOKEN=your-token-here
BILLING_ASSISTANT_TIMEOUT=15

# Application
SECRET_KEY=generate-a-random-secret-here
DATABASE_PATH=memory.db

# Internal API auth (billing → assistant-core)
ASSISTANT_CORE_API_TOKEN=generate-a-random-token-here

# Anthropic API key (for account analysis and other AI features)
ANTHROPIC_API_KEY=sk-ant-...

# Admin dashboard
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=          # bcrypt hash — generate with: python scripts/generate_admin_password.py
ADMIN_SECRET_KEY=             # random string for cookie signing — generate with: python -c "import secrets; print(secrets.token_hex(32))"
# GIT_SSH_AUTH_SOCK=           # optional: path to persistent ssh-agent socket for git pull from dashboard
                               # e.g. /run/user/1000/keyring/ssh — only needed if NOT using a passphrase-free deploy key

# System prompt (optional override)
# SYSTEM_PROMPT=You are a helpful AI assistant.
